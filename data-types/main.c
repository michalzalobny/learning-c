#include <stdio.h>
#include <stdlib.h>

int main(){
  int age = 40;
  double gpa = 3.7;
  char grade = 'A'; //Char always in single quotation marks
  char phrase[] = "The academy"; //for strings we need the dobule quotations marks
  return 0;
}