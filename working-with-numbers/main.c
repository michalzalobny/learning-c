#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
  //%d -> integer; %s -> string, %f-> decimal, %c -> single chars
  printf("%f\n", 5 - 4.5); //It will convert to double
  printf("%f \n", sqrt(4.0));
  printf("%f", floor(36.765));
  return 0;
}